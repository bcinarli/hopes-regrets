/**
 * // Page Controller
 */
(function(window){
    "use strict";

    // Get namespaces.
    var BNHE        = window.BenNeHaltEttim,
        controllers = BNHE.modules.controllers;

    // Controller function.
    var Controller = function($scope){

    };

    // Register controller.
    controllers.controller("bnheWelcomeCtrl", ["$scope", Controller]);

})(window);